# lab_2

## 📝 Описание задания:
Общее требование: _меню пользователя должно содержать возможность редактирования любой записи любого объекта, удаление любого объекта, добавления нового объекта на любую позицию_.

<br>

<b>Задание 1: Стандартные потоки (Расписание поездов)</b>

Разработать систему для управления расписанием поездов. Для этого необходимо:

1. Определить класс TRAIN со следующими полями: название пункта назначения, номер поезда, время отправления.
2. Реализовать методы доступа к полям (get/set), все виды конструкторов, деструктор, а также перегрузить операторы потокового ввода/вывода (<<, >>) и присваивания (=).
3. Число поездов в расписании заранее не известно, поэтому необходимо использовать динамическое выделение памяти. Использование контейнеров STL запрещено.
4. Программа должна обеспечивать следующий функционал:
* Добавление поезда в расписание с автоматической сортировкой по времени отправления.
* Редактирование и удаление поездов по индексу.
* Вывод на экран информации о поездах, направляющихся в пункт назначения, введенный пользователем.
* В случае отсутствия таких поездов, выводить соответствующее сообщение.

<br>

<b>Задание 2: Файловые и строковые потоки (Поиск цитат)</b>

Написать программу, которая считывает весь текст из файла input.txt и выводит на экран только цитаты — предложения, заключенные в двойные кавычки (").

## 📊 Таблица описания классов
| Имя класса | Назначение | Наследование | Тип | Поля | Методы |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `Train` | Модель данных, хранящая информацию об одном поезде. | - | Базовый | `char* destination` <br> `int trainNumber` <br> `char* departureTime` | Конструкторы (по умолч., с парам., копир.), деструктор, `get`/`set` для всех полей, перегруженные операторы `<<`, `>>`, `=`. |
| `TrainSchedule` | Управление коллекцией объектов `Train`. | - | Управляющий | `Train* trains` <br> `int size` <br> `int capacity` | Конструктор, деструктор, `addTrain`, `deleteTrain`, `editTrain`, `displayAll`, `findByDestination`, `resize`. |
| `CustomException` | Пользовательский класс для генерации и обработки исключительных ситуаций. | `std::exception` | Вспомогательный | `std::string message` | Конструктор, `what()`. |

## 📐 Иерархия связи классов
<h1 align="center">
  <img src="https://s.iimg.su/s/05/gAmouQLxFZ7gerq2TVMJN6z5qypt3jZAX75dlp61.png">
</h1>

## 📂 Архитектура проекта
Проект имеет модульную структуру, которая разделяет публичный интерфейс (заголовочные файлы) и его внутреннюю реализацию (исходные файлы).

```
lab_2/                     # Папка для публичных заголовочных файлов (.h)
├── include/
│   ├── CustomException.h
│   ├── Train.h
│   ├── TrainSchedule.h
│   └── tasks.h
│
├── src/                   # Папка для файлов реализации (.cpp)
│   ├── Train.cpp
│   ├── TrainSchedule.cpp
│   ├── task1.cpp
│   ├── task2.cpp
│   └── main.cpp           # Главный файл, с которого начинается выполнение программы
│
├── .gitignore             # Специальный файл для системы контроля версий Git
├── CMakeLists.txt         # Главный конфигурационный файл для системы сборки CMake
└── README.md              # Этот файл. Документация проекта
```
